{
  "name": "aerchain-e2e-playwrite",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "npx playwright test",
    "test:slack": "npx playwright test || true && node utils/sendReports.js",
    "test:full": "rm -rf docs && mkdir docs && rm -rf test-results playwright-report && npx playwright test || true && cp -r playwright-report/* docs/ && git add . && git commit -m 'update report' || echo 'No changes' && git push && node utils/sendReports.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  },
  "dependencies": {
    "axios": "^1.13.6",
    "dotenv": "^17.3.1",
    "nodemailer": "^8.0.4"
  }
}
